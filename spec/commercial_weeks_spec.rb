require "spec_helper"

describe CommercialWeeks do
  it "has a version number" do
    expect(CommercialWeeks::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
